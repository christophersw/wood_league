"""App configuration for the dashboard Django application."""

from django.apps import AppConfig


class DashboardConfig(AppConfig):
    """Configuration class for the dashboard app."""

    name = 'dashboard'
