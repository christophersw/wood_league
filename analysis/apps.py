"""Django app configuration for the analysis module."""
from django.apps import AppConfig


class AnalysisConfig(AppConfig):
    """Configuration for the analysis Django app."""
    name = 'analysis'
